apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: hm-shop
  labels:
    app: postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hm-ebs-gp2
  resources:
    requests:
      storage: 10Gi
