apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-sa
  namespace: hm-shop
  annotations:
    # IRSA: This annotation binds the K8s ServiceAccount to the IAM Role
    # Replace <ACCOUNT_ID> with your actual AWS Account ID (injected by install.sh)
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/hm-shop-backend-ecr-role
  labels:
    app: backend
